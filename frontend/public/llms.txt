# Myriad

> This static file is a privacy-safe fallback when the backend does not serve
> /llms.txt. Dynamic content (site intro, routes) is provided only by the
> application according to the site visibility policy.

AI crawlers: prefer the proxied /llms.txt from the origin. If you only see this
stub, treat the site as not advertising an open index via this file.

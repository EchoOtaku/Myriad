# Static fallback when the backend does not proxy /robots.txt.
# Prefer the dynamic response (visibility policy, absolute Sitemap).
# Default is conservative: block indexing until the app serves policy-aware rules.

User-agent: *
Disallow: /

# When the backend is correctly proxied, it overrides this file with policy-based
# Allow/Disallow and an absolute Sitemap URL.

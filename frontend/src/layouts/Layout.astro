---
interface Props {
  title: string;
  description?: string;
}

const { title, description = 'Myriad - Multi-Platform Profile Aggregator' } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content={description} />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap"
      rel="stylesheet"
    />
  </head>
  <body class="antialiased">
    <!-- Animated Background -->
    <div id="bg-container" class="fixed inset-0 -z-10 overflow-hidden">
      <!-- Dynamic Wallpaper -->
      <div id="wallpaper" class="absolute inset-0 bg-cover bg-center bg-no-repeat"></div>
      
      <!-- Gradient from middle: Top transparent, Bottom white blur -->
      <div class="absolute inset-0 bg-gradient-to-b from-transparent from-[45%] via-white/60 via-[50%] to-white/95"></div>
      
      <!-- Subtle Mesh Gradient - Lower half only -->
      <div class="absolute inset-0 opacity-20">
        <div class="absolute top-[45%] left-10 w-96 h-96 bg-green-400/40 rounded-full mix-blend-multiply filter blur-3xl animate-blob"></div>
        <div class="absolute top-[45%] right-10 w-96 h-96 bg-pink-400/40 rounded-full mix-blend-multiply filter blur-3xl animate-blob animation-delay-2000"></div>
      </div>
      
      <!-- Subtle Grid Pattern -->
      <div class="absolute inset-0 bg-grid-pattern opacity-[0.02]"></div>
    </div>

    <!-- Dynamic Island Navigation - Left Side Vertical -->
    <nav class="fixed left-6 top-1/2 -translate-y-1/2 z-50">
      <div class="dynamic-island shadow-2xl">
        <div class="flex flex-col items-center gap-3">
          <a href="/" class="nav-item" title="主页">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"></path>
            </svg>
          </a>
          
          <a href="/config" class="nav-item" title="配置">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"></path>
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
            </svg>
          </a>

          <!-- 分隔线 -->
          <div class="w-8 h-px bg-gradient-to-r from-transparent via-gray-300 to-transparent"></div>

          <!-- 登录/用户头像按钮 -->
          <div id="user-section">
            <!-- 未登录状态 - 显示登录按钮 -->
            <button id="login-btn" class="nav-item" title="登录" style="display: none;">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
              </svg>
            </button>
            
            <!-- 已登录状态 - 显示用户头像 -->
            <div id="user-avatar-container" class="relative" style="display: none;">
              <button id="user-avatar-btn" class="nav-item p-0 w-10 h-10 overflow-hidden" title="用户菜单">
                <img id="user-avatar" src="" alt="User" class="w-full h-full object-cover rounded-full">
              </button>
              
              <!-- 用户下拉菜单 -->
              <div id="user-menu" class="absolute left-full ml-3 top-0 glass rounded-xl shadow-2xl p-2 min-w-[180px] hidden z-50">
                <div class="px-3 py-2 border-b border-gray-200">
                  <p class="text-sm font-semibold text-gray-800" id="user-name">用户名</p>
                  <p class="text-xs text-gray-500">@<span id="user-username">username</span></p>
                </div>
                <button id="logout-btn" class="w-full text-left px-3 py-2 text-sm text-red-600 hover:bg-red-50 rounded-lg transition-colors mt-1">
                  <svg class="w-4 h-4 inline mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"></path>
                  </svg>
                  退出登录
                </button>
              </div>
            </div>
          </div>

          <!-- 分隔线 -->
          <div class="w-8 h-px bg-gradient-to-r from-transparent via-gray-300 to-transparent"></div>

          <!-- 生成报告按钮 -->
          <button id="generate-report-btn" class="nav-item generate-btn" title="生成报告">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"></path>
            </svg>
          </button>
        </div>
      </div>
    </nav>

    <main class="relative z-10">
      <slot />
    </main>

    <style is:global>
      :root {
        --color-primary: #94a3b8;
        --color-secondary: #94a3b8;
        --color-accent: #cbd5e1;
        --color-light: #cbd5e1;
        --color-dark: #475569;
        
        /* 颜色切换平滑过渡 */
        transition: --color-primary 0.6s ease-in-out,
                    --color-secondary 0.6s ease-in-out,
                    --color-accent 0.6s ease-in-out,
                    --color-light 0.6s ease-in-out,
                    --color-dark 0.6s ease-in-out;
      }
      
      @supports not (transition: --color-primary 0.6s) {
        /* 对于不支持 CSS 变量过渡的浏览器，使用通配符 */
        * {
          transition: color 0.6s ease-in-out,
                      background-color 0.6s ease-in-out,
                      border-color 0.6s ease-in-out,
                      box-shadow 0.6s ease-in-out;
        }
      }

      @keyframes blob {
        0%, 100% {
          transform: translate(0, 0) scale(1);
        }
        33% {
          transform: translate(30px, -50px) scale(1.1);
        }
        66% {
          transform: translate(-20px, 20px) scale(0.9);
        }
      }

      .animate-blob {
        animation: blob 7s infinite;
      }

      .animation-delay-2000 {
        animation-delay: 2s;
      }

      .animation-delay-4000 {
        animation-delay: 4s;
      }

      .bg-grid-pattern {
        background-image: 
          linear-gradient(rgba(255, 255, 255, 0.05) 1px, transparent 1px),
          linear-gradient(90deg, rgba(255, 255, 255, 0.05) 1px, transparent 1px);
        background-size: 50px 50px;
      }

      body {
        font-family: 'Inter', sans-serif;
        background: #fef3c7;
        color: #1f2937;
      }

      /* Glass effect utility - Bright style */
      .glass {
        background: rgba(255, 255, 255, 0.7);
        backdrop-filter: blur(12px);
        -webkit-backdrop-filter: blur(12px);
        border: 1px solid rgba(255, 255, 255, 0.3);
        box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.1);
      }

      /* Dynamic Island - 灵动岛样式（垂直布局，流动玻璃材质） */
      .dynamic-island {
        background: rgba(255, 255, 255, 0.75);
        backdrop-filter: blur(20px);
        -webkit-backdrop-filter: blur(20px);
        border-radius: 24px;
        padding: 16px 12px;
        border: 2px solid color-mix(in srgb, var(--color-primary) 40%, transparent);
        box-shadow: 0 8px 32px 0 color-mix(in srgb, var(--color-primary) 15%, transparent),
                    inset 0 1px 0 0 rgba(255, 255, 255, 0.8);
        transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        position: relative;
        overflow: hidden;
      }

      /* 动态渐变边框动画 */
      .dynamic-island::before {
        content: '';
        position: absolute;
        inset: -2px;
        border-radius: 24px;
        padding: 2px;
        background: linear-gradient(135deg, 
          color-mix(in srgb, var(--color-primary) 60%, transparent),
          color-mix(in srgb, var(--color-secondary) 40%, transparent),
          color-mix(in srgb, var(--color-accent) 40%, transparent),
          color-mix(in srgb, var(--color-primary) 60%, transparent)
        );
        -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
        -webkit-mask-composite: xor;
        mask-composite: exclude;
        animation: border-flow 8s linear infinite;
        background-size: 300% 300%;
      }

      @keyframes border-flow {
        0%, 100% {
          background-position: 0% 50%;
        }
        50% {
          background-position: 100% 50%;
        }
      }

      .dynamic-island:hover {
        padding: 18px 12px;
        background: rgba(255, 255, 255, 0.85);
        box-shadow: 0 12px 48px 0 color-mix(in srgb, var(--color-primary) 25%, transparent),
                    inset 0 1px 0 0 rgba(255, 255, 255, 0.9);
      }

      /* Navigation items */
      .nav-item {
        @apply text-gray-500 transition-all duration-300;
        @apply transform hover:scale-110 active:scale-95;
        @apply relative p-2.5;
        border-radius: 16px;
      }

      .nav-item:hover {
        color: var(--color-primary);
        background: color-mix(in srgb, var(--color-primary) 10%, transparent);
      }

      /* Active nav item indicator */
      [aria-current="page"].nav-item {
        color: var(--color-primary);
        background: color-mix(in srgb, var(--color-primary) 10%, white);
      }

      /* Generate button with gradient border - 简洁旋转版 */
      .generate-btn {
        position: relative;
        background: linear-gradient(135deg, 
          color-mix(in srgb, var(--color-primary) 15%, transparent), 
          color-mix(in srgb, var(--color-secondary) 15%, transparent)
        );
        border: 2px solid transparent;
        cursor: pointer;
        z-index: 1;
        border-radius: 16px;
        /* 硬件加速优化 */
        transform: translateZ(0);
        backface-visibility: hidden;
        transition: border-radius 0.3s ease;
      }

      /* 加载时变圆形 */
      .generate-btn.loading {
        border-radius: 50%;
      }

      .generate-btn svg {
        position: relative;
        z-index: 2;
        pointer-events: none;
      }

      /* 边框容器 */
      .generate-btn::before {
        content: '';
        position: absolute;
        inset: -2px;
        border-radius: 16px;
        z-index: -1;
        pointer-events: none;
        overflow: hidden;
        transition: border-radius 0.3s ease;
      }

      .generate-btn.loading::before {
        border-radius: 50%;
      }

      /* 渐变层 - 旋转 */
      .generate-btn::after {
        content: '';
        position: absolute;
        inset: -2px;
        border-radius: 16px;
        padding: 2px;
        z-index: -1;
        pointer-events: none;
        background: conic-gradient(
          from 0deg,
          rgba(34, 197, 94, 0.9),    /* 绿 */
          rgba(251, 191, 36, 0.9),   /* 黄 */
          rgba(236, 72, 153, 0.9),   /* 粉 */
          rgba(139, 92, 246, 0.9),   /* 紫 */
          rgba(59, 130, 246, 0.9),   /* 蓝 */
          rgba(16, 185, 129, 0.9),   /* 青绿 */
          rgba(34, 197, 94, 0.9)     /* 绿（循环） */
        );
        -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
        -webkit-mask-composite: xor;
        mask-composite: exclude;
        /* 硬件加速 */
        will-change: transform;
        transform: translateZ(0) rotate(0deg);
        backface-visibility: hidden;
        transition: border-radius 0.3s ease;
      }

      .generate-btn.loading::after {
        border-radius: 50%;
        animation: rainbow-spin 1.5s linear infinite;
      }

      /* 彩虹边框旋转 */
      @keyframes rainbow-spin {
        0% {
          transform: translateZ(0) rotate(0deg);
        }
        100% {
          transform: translateZ(0) rotate(360deg);
        }
      }

      .generate-btn:hover {
        background: linear-gradient(135deg, 
          color-mix(in srgb, var(--color-primary) 25%, transparent), 
          color-mix(in srgb, var(--color-secondary) 25%, transparent)
        );
        transform: scale(1.15);
      }

      .generate-btn:active {
        transform: scale(1.05);
      }

      .generate-btn.loading::before {
        animation: rainbow-spin 2s linear infinite;
      }

      /* Shuffle button - 使用壁纸提取的颜色渐变 */
      .shuffle-btn {
        position: relative;
        background: linear-gradient(135deg, 
          color-mix(in srgb, var(--color-primary) 15%, transparent), 
          color-mix(in srgb, var(--color-secondary) 15%, transparent)
        );
        border: 2px solid transparent;
        cursor: pointer;
        z-index: 1;
        color: var(--color-dark);
        font-weight: 500;
        /* 硬件加速优化 */
        transform: translateZ(0);
        backface-visibility: hidden;
      }

      .shuffle-btn::before {
        content: '';
        position: absolute;
        inset: -2px;
        border-radius: 16px;
        z-index: -1;
        pointer-events: none;
        overflow: hidden;
      }

      .shuffle-btn::after {
        content: '';
        position: absolute;
        inset: -2px;
        border-radius: 16px;
        padding: 2px;
        z-index: -1;
        pointer-events: none;
        /* 使用壁纸提取的 primary 和 secondary 颜色 */
        background: linear-gradient(
          135deg,
          var(--color-primary) 0%,
          var(--color-secondary) 100%
        );
        -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
        -webkit-mask-composite: xor;
        mask-composite: exclude;
        will-change: transform;
        transform: translateZ(0);
        backface-visibility: hidden;
        /* 平滑过渡颜色变化 */
        transition: background 0.6s ease-in-out;
      }

      .shuffle-btn:hover {
        background: linear-gradient(135deg, 
          color-mix(in srgb, var(--color-primary) 25%, transparent), 
          color-mix(in srgb, var(--color-secondary) 25%, transparent)
        );
        box-shadow: 0 4px 15px color-mix(in srgb, var(--color-primary) 30%, transparent);
      }

      /* Smooth scrolling */
      html {
        scroll-behavior: smooth;
      }

      /* Custom scrollbar - Spring theme */
      ::-webkit-scrollbar {
        width: 8px;
        height: 8px;
      }
      ::-webkit-scrollbar-track {
        background: rgba(254, 243, 199, 0.5);
      }
      ::-webkit-scrollbar-thumb {
        background: color-mix(in srgb, var(--color-primary) 50%, transparent);
        border-radius: 4px;
      }
      ::-webkit-scrollbar-thumb:hover {
        background: color-mix(in srgb, var(--color-secondary) 70%, transparent);
      }

      /* 动态颜色样式 - 替代内联样式 */
      .progress-bar-fill {
        background-color: var(--color-primary);
        box-shadow: 0 0 10px color-mix(in srgb, var(--color-primary) 50%, transparent);
      }

      /* 进度条宽度通过 JavaScript 动态设置 */
      .progress-bar-fill[data-progress] {
        width: calc(var(--progress, 0) * 1%);
      }

      .report-info-card {
        border-color: color-mix(in srgb, var(--color-primary) 30%, transparent);
      }

      .platform-indicator {
        background-color: var(--color-primary);
      }

      .report-card {
        border-color: color-mix(in srgb, var(--color-primary) 20%, transparent);
        box-shadow: 0 4px 20px color-mix(in srgb, var(--color-primary) 10%, transparent);
      }

      .tag-badge {
        background-color: color-mix(in srgb, var(--color-primary) 70%, transparent);
        backdrop-filter: blur(8px);
        -webkit-backdrop-filter: blur(8px);
        border: 1px solid color-mix(in srgb, var(--color-primary) 30%, transparent);
      }

      .modal-card {
        border-color: color-mix(in srgb, var(--color-primary) 30%, transparent);
      }

      .icon-primary {
        color: var(--color-primary);
      }

      .ai-insight-box {
        background-color: color-mix(in srgb, var(--color-primary) 10%, transparent);
        border-left: 4px solid var(--color-primary);
        border-color: color-mix(in srgb, var(--color-primary) 20%, transparent);
      }

      .ai-insight-border {
        border-color: transparent;
      }

      .btn-primary-large {
        background-color: var(--color-primary);
        box-shadow: 0 4px 14px color-mix(in srgb, var(--color-primary) 40%, transparent);
      }

      .btn-secondary-large {
        background-color: var(--color-secondary);
        box-shadow: 0 4px 14px color-mix(in srgb, var(--color-secondary) 40%, transparent);
      }

      .btn-primary {
        background-color: var(--color-primary);
      }

      .btn-secondary {
        background-color: var(--color-secondary);
      }

      /* 弹窗动画 */
      @keyframes fade-in {
        from {
          opacity: 0;
        }
        to {
          opacity: 1;
        }
      }

      @keyframes scale-in {
        from {
          opacity: 0;
          transform: scale(0.95);
        }
        to {
          opacity: 1;
          transform: scale(1);
        }
      }

      @keyframes pulse-slow {
        0%, 100% {
          opacity: 1;
        }
        50% {
          opacity: 0.7;
        }
      }

      .animate-fade-in {
        animation: fade-in 0.2s ease-out;
      }

      .animate-scale-in {
        animation: scale-in 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
      }

      .animate-pulse-slow {
        animation: pulse-slow 2s ease-in-out infinite;
      }

      /* 平台链接样式 */
      .platform-link {
        background: linear-gradient(135deg, 
          color-mix(in srgb, var(--brand-color) 7%, transparent), 
          color-mix(in srgb, var(--brand-color) 4%, transparent)
        );
        border: 1.5px solid color-mix(in srgb, var(--brand-color) 20%, transparent);
        color: var(--brand-color);
        transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1);
      }

      .platform-link:hover {
        background: linear-gradient(135deg, 
          color-mix(in srgb, var(--brand-color) 13%, transparent), 
          color-mix(in srgb, var(--brand-color) 9%, transparent)
        );
        border-color: color-mix(in srgb, var(--brand-color) 30%, transparent);
        transform: translateY(-2px) scale(1.05);
        box-shadow: 0 6px 16px color-mix(in srgb, var(--brand-color) 15%, transparent), 
                    0 0 0 1px color-mix(in srgb, var(--brand-color) 9%, transparent);
      }

      /* 自定义滚动条 */
      .custom-scrollbar::-webkit-scrollbar {
        width: 6px;
      }

      .custom-scrollbar::-webkit-scrollbar-track {
        background: transparent;
        margin: 8px 0;
      }

      .custom-scrollbar::-webkit-scrollbar-thumb {
        background: color-mix(in srgb, var(--color-primary) 40%, transparent);
        border-radius: 10px;
      }

      .custom-scrollbar::-webkit-scrollbar-thumb:hover {
        background: color-mix(in srgb, var(--color-primary) 60%, transparent);
      }

      /* 渐变工具类 */
      .via-primary {
        --tw-gradient-stops: var(--tw-gradient-from), var(--color-primary), var(--tw-gradient-to);
      }

      /* 萌宠走动动画 - 精灵图 */
      .pet-walker {
        transition: left 0.05s linear, transform 0.3s ease;
        display: flex;
        align-items: flex-end; /* 底部对齐 */
      }

      .pet-sprite {
        background-image: var(--pet-image-url, url('https://api.fuukei.org/myriad/frontend/public/furina.png'));
        background-size: 100% 100%; /* 填充整个容器 */
        background-repeat: no-repeat;
        background-position: center bottom; /* 底部对齐 */
        image-rendering: pixelated;
        image-rendering: -moz-crisp-edges;
        image-rendering: crisp-edges;
        filter: drop-shadow(0 2px 4px rgba(0,0,0,0.2));
        transition: transform 0.3s ease;
        width: 100%;
        height: 100%;
      }

      .pet-sprite:hover {
        transform: scale(1.1);
      }

      .pixelated-image {
        image-rendering: pixelated;
        image-rendering: -moz-crisp-edges;
        image-rendering: crisp-edges;
      }

      .pet-walker.walking .pet-sprite {
        animation: pet-walk-bounce 0.5s ease-in-out infinite;
      }

      @keyframes pet-walk-bounce {
        0%, 100% {
          transform: translateY(0) scaleY(1);
        }
        25% {
          transform: translateY(-3px) scaleY(1.02) scaleX(0.98);
        }
        50% {
          transform: translateY(0) scaleY(0.98) scaleX(1.02);
        }
        75% {
          transform: translateY(-3px) scaleY(1.02) scaleX(0.98);
        }
      }

      @keyframes pet-excited {
        0% {
          transform: translateY(0) rotate(0deg);
        }
        30% {
          transform: translateY(-12px) rotate(-8deg) scale(1.1);
        }
        60% {
          transform: translateY(-16px) rotate(8deg) scale(1.1);
        }
        100% {
          transform: translateY(0) rotate(0deg);
        }
      }

      .pet-walker.pet-excited {
        animation: pet-excited 0.6s ease-out;
      }

      @keyframes pet-walk-shadow {
        0%, 100% {
          filter: drop-shadow(0 2px 4px rgba(0,0,0,0.2));
        }
        50% {
          filter: drop-shadow(0 3px 6px rgba(0,0,0,0.25));
        }
      }
    </style>

    <script>
      import { extractColorsFromImage, applyColorPalette } from '../utils/colorExtractor';

      // 全局状态：存储当前显示的壁纸的实际 URL（重定向后的最终 URL）
      let activeWallpaperUrl: string | null = null;

      // 缓存接口
      interface WallpaperCache {
        apiUrl: string;
        actualUrl: string;
        timestamp: number;
      }

      // 从 localStorage 获取上次的壁纸缓存
      function getLastWallpaperCache(): WallpaperCache | null {
        try {
          const cached = localStorage.getItem('lastWallpaper');
          if (!cached) return null;
          const data = JSON.parse(cached) as WallpaperCache;
          // 缓存 1 小时内有效
          if (Date.now() - data.timestamp < 60 * 60 * 1000) {
            return data;
          }
          return null;
        } catch {
          return null;
        }
      }

      // 保存壁纸缓存
      function saveWallpaperCache(apiUrl: string, actualUrl: string): void {
        try {
          const cache: WallpaperCache = { apiUrl, actualUrl, timestamp: Date.now() };
          localStorage.setItem('lastWallpaper', JSON.stringify(cache));
        } catch (e) {
          console.warn('⚠️ [Layout] Failed to save wallpaper cache:', e);
        }
      }

      // 获取图片的实际 URL（处理重定向）
      async function getActualImageUrl(apiUrl: string): Promise<string> {
        try {
          // 先获取实际的图片 URL（通过 fetch 会自动跟随重定向）
          const response = await fetch(apiUrl, { method: 'HEAD' });
          const actualUrl = response.url; // 重定向后的最终 URL
          console.log('🔗 [Layout] API URL:', apiUrl);
          console.log('🔗 [Layout] Actual image URL:', actualUrl);
          return actualUrl;
        } catch {
          // 如果获取失败，回退到原始 URL
          return apiUrl;
        }
      }

      // 立即显示缓存的壁纸和颜色
      function showCachedWallpaper(wallpaperEl: HTMLElement, cache: WallpaperCache, blur: number): void {
        console.log('⚡ [Layout] Using cached wallpaper for instant display');
        wallpaperEl.style.backgroundImage = `url(${cache.actualUrl})`;
        wallpaperEl.style.filter = `blur(${blur}px)`;
        activeWallpaperUrl = cache.actualUrl;
        
        // 立即提取并应用缓存的颜色（从 colorExtractor 的缓存）
        extractColorsFromImage(cache.actualUrl).then(colors => {
          if (activeWallpaperUrl === cache.actualUrl) {
            applyColorPalette(colors);
            console.log('⚡ [Layout] Cached colors applied instantly:', colors);
          }
        });
      }

      // Load wallpaper and extract colors
      async function loadWallpaperAndColors() {
        try {
          const response = await fetch('http://localhost:3000/api/config');
          const data = await response.json();
          const wallpaperEl = document.getElementById('wallpaper');
          
          if (wallpaperEl && data.ui_config?.wallpaper_url) {
            const apiUrl = data.ui_config.wallpaper_url;
            const blur = data.ui_config.wallpaper_blur || 3;
            
            console.log('🖼️ [Layout] Loading wallpaper from API:', apiUrl);
            
            // 🚀 优化1: 先检查缓存，立即显示
            const cachedWallpaper = getLastWallpaperCache();
            if (cachedWallpaper && cachedWallpaper.apiUrl === apiUrl) {
              showCachedWallpaper(wallpaperEl, cachedWallpaper, blur);
              console.log('✅ [Layout] Cached wallpaper displayed instantly');
            }
            
            // 🚀 优化2: 后台获取新的实际 URL
            const actualImageUrl = await getActualImageUrl(apiUrl);
            
            // 如果实际 URL 和缓存的一样，不需要更新
            if (cachedWallpaper && cachedWallpaper.actualUrl === actualImageUrl) {
              console.log('✅ [Layout] Wallpaper unchanged, no update needed');
              return;
            }
            
            // URL 不同，需要更新
            console.log('🔄 [Layout] New wallpaper detected, updating...');
            activeWallpaperUrl = actualImageUrl;
            
            // 🚀 优化3：并行加载新图片和提取颜色
            const img = new Image();
            img.crossOrigin = 'anonymous';
            
            console.log('🎨 [Layout] Starting color extraction (parallel)');
            const colorPromise = extractColorsFromImage(actualImageUrl);
            
            // 加载新图片
            new Promise<void>((resolve, reject) => {
              img.onload = () => {
                if (activeWallpaperUrl === actualImageUrl) {
                  wallpaperEl.style.backgroundImage = `url(${actualImageUrl})`;
                  wallpaperEl.style.filter = `blur(${blur}px)`;
                  console.log('✅ [Layout] New wallpaper displayed');
                  resolve();
                } else {
                  console.warn('⚠️ [Layout] Wallpaper URL changed, skipping display');
                  reject(new Error('URL changed'));
                }
              };
              img.onerror = () => {
                console.error('❌ [Layout] Failed to load wallpaper');
                reject(new Error('Failed to load wallpaper'));
              };
              img.src = actualImageUrl;
            });
            
            // 等待颜色提取完成
            const result = await colorPromise;
            
            if (activeWallpaperUrl === actualImageUrl) {
              applyColorPalette(result);
              console.log('✅ [Layout] New colors applied:', result);
              
              // 保存新的缓存
              saveWallpaperCache(apiUrl, actualImageUrl);
            } else {
              console.warn('⚠️ [Layout] Wallpaper changed during extraction');
              console.warn(`   Extracted for: ${actualImageUrl}`);
              console.warn(`   But active is: ${activeWallpaperUrl}`);
              console.warn('   Colors NOT applied');
            }
            
          } else {
            // 没有配置壁纸，使用默认配色
            console.log('ℹ️ [Layout] No wallpaper configured, using default colors');
          }
        } catch (error) {
          console.error('❌ [Layout] Error loading wallpaper/colors:', error);
          // 出错时保持默认配色，不清除颜色
        }
      }
      
      // Set active nav state
      function setActiveNav() {
        const currentPath = window.location.pathname;
        const navItems = document.querySelectorAll('.nav-item:not(.generate-btn)');
        
        navItems.forEach(item => {
          const href = item.getAttribute('href');
          if (href === currentPath || (currentPath === '/' && href === '/')) {
            item.setAttribute('aria-current', 'page');
          } else {
            item.removeAttribute('aria-current');
          }
        });
      }

      // Handle generate report button
      function setupGenerateButton() {
        const generateBtn = document.getElementById('generate-report-btn');
        if (!generateBtn) return;

        generateBtn.addEventListener('click', async (e) => {
          e.preventDefault();
          e.stopPropagation();
          
          // 检查是否在首页
          if (window.location.pathname !== '/') {
            window.location.href = '/';
            return;
          }

          // 调用 ReportCards 组件的生成函数
          if (typeof (window as any).generateReport === 'function') {
            generateBtn.classList.add('loading');
            try {
              await (window as any).generateReport();
            } catch (err) {
              console.error('Report generation failed:', err);
            } finally {
              generateBtn.classList.remove('loading');
            }
          }
        });
      }
      
      loadWallpaperAndColors();
      setActiveNav();
      setupGenerateButton();
      setupAuthUI();

      // 检查URL中的token参数（OAuth回调）
      const urlParams = new URLSearchParams(window.location.search);
      const token = urlParams.get('token');
      if (token) {
        localStorage.setItem('auth_token', token);
        // 清除URL中的token参数
        window.history.replaceState({}, document.title, window.location.pathname);
        // 刷新用户信息
        loadUserInfo();
      }

      // 用户认证UI设置
      function setupAuthUI() {
        loadUserInfo();

        const loginBtn = document.getElementById('login-btn');
        const userAvatarBtn = document.getElementById('user-avatar-btn');
        const userMenu = document.getElementById('user-menu');
        const logoutBtn = document.getElementById('logout-btn');

        // 登录按钮点击
        loginBtn?.addEventListener('click', async () => {
          try {
            // 先检查后端OAuth是否配置
            const response = await fetch('http://localhost:3000/api/auth/github/login');
            
            if (!response.ok) {
              const error = await response.json();
              alert(`登录失败：${error.message || error.error || '服务器错误'}\n\n请联系管理员配置 GitHub OAuth。`);
              console.error('GitHub OAuth error:', error);
              return;
            }
            
            // 如果返回重定向，直接跳转
            window.location.href = 'http://localhost:3000/api/auth/github/login';
          } catch (e) {
            console.error('Login error:', e);
            alert('无法连接到认证服务器，请确保后端服务正在运行。');
          }
        });

        // 用户头像点击 - 切换菜单
        userAvatarBtn?.addEventListener('click', (e) => {
          e.stopPropagation();
          userMenu?.classList.toggle('hidden');
        });

        // 点击其他地方关闭菜单
        document.addEventListener('click', () => {
          userMenu?.classList.add('hidden');
        });

        // 退出登录
        logoutBtn?.addEventListener('click', async () => {
          try {
            await fetch('http://localhost:3000/api/auth/logout', {
              method: 'POST',
              headers: {
                'Authorization': `Bearer ${localStorage.getItem('auth_token')}`
              }
            });
          } catch (e) {
            console.error('Logout request failed:', e);
          }
          
          localStorage.removeItem('auth_token');
          window.location.reload();
        });
      }

      // 加载用户信息
      async function loadUserInfo() {
        const token = localStorage.getItem('auth_token');
        const loginBtn = document.getElementById('login-btn');
        const userAvatarContainer = document.getElementById('user-avatar-container');
        
        if (!token) {
          // 未登录状态
          loginBtn!.style.display = 'block';
          userAvatarContainer!.style.display = 'none';
          // 触发自定义事件通知其他组件
          window.dispatchEvent(new CustomEvent('auth-state-changed', { 
            detail: { isAuthenticated: false } 
          }));
          return;
        }

        try {
          const response = await fetch('http://localhost:3000/api/auth/me', {
            headers: {
              'Authorization': `Bearer ${token}`
            }
          });

          if (!response.ok) {
            throw new Error('Failed to get user info');
          }

          const user = await response.json();
          
          // 已登录状态 - 显示用户头像
          loginBtn!.style.display = 'none';
          userAvatarContainer!.style.display = 'block';
          
          const userAvatar = document.getElementById('user-avatar') as HTMLImageElement;
          const userName = document.getElementById('user-name');
          const userUsername = document.getElementById('user-username');
          
          if (userAvatar) userAvatar.src = user.avatar_url || 'https://github.com/ghost.png';
          if (userName) userName.textContent = user.display_name || user.username;
          if (userUsername) userUsername.textContent = user.username;

          // 触发自定义事件通知其他组件
          window.dispatchEvent(new CustomEvent('auth-state-changed', { 
            detail: { isAuthenticated: true, user } 
          }));
          
        } catch (error) {
          console.error('Failed to load user info:', error);
          // Token无效，清除并显示登录按钮
          localStorage.removeItem('auth_token');
          loginBtn!.style.display = 'block';
          userAvatarContainer!.style.display = 'none';
          window.dispatchEvent(new CustomEvent('auth-state-changed', { 
            detail: { isAuthenticated: false } 
          }));
        }
      }
    </script>
  </body>
</html>

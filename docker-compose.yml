# Myriad Docker Compose 配置文件 (v2 — proxy + updater)
#
# ============================================================================
# 概述
# ============================================================================
# 这个 compose 文件配合 Myriad updater 系统使用。架构：
#
#   proxy (80) ──┬──► frontend
#                └──► backend ──► postgres
#   updater (内网) — 负责拉取镜像/快照 pgdata/切换版本
#
# 升级流程见 docs/updater-spec.md。
# 老版本（直接暴露 backend/frontend 端口、使用 :latest 镜像）已废弃，
# 迁移请使用 scripts/migrate-to-updater.sh。
#
# ============================================================================
# 必需 .env 变量
# ============================================================================
# MYRIAD_TAG             业务镜像版本，由 updater 维护
# PROXY_TAG              代理镜像版本（少有变化）
# UPDATER_TAG            updater 镜像版本（独立节奏）
# COMPOSE_PROJECT_NAME=myriad  （updater 依赖这个）
#
# POSTGRES_PASSWORD      数据库密码（≥32 字符）
# JWT_SECRET             JWT 密钥（≥32 字符）
# CORS_ORIGINS           生产环境必填
#
# UPDATE_TOKEN           updater 鉴权 token（≥32 字符）
# CHANNEL                stable | beta | nightly
# GITHUB_TOKEN           可选：提升 GitHub API rate limit
# REGISTRY_MIRROR        可选：镜像 mirror 前缀
# MYRIAD_GITHUB_REPO     默认 somekawahitomi/myriad
# ============================================================================

services:
  postgres:
    image: postgres:16-alpine
    container_name: myriad-postgres
    deploy:
      resources:
        limits: { cpus: '2.0', memory: 2G }
        reservations: { cpus: '0.5', memory: 512M }
    environment:
      POSTGRES_DB: myriad
      POSTGRES_USER: myriad
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_INITDB_ARGS: "-E UTF8 --locale=C --lc-collate=C --lc-ctype=C"
      POSTGRES_SHARED_BUFFERS: 512MB
      POSTGRES_EFFECTIVE_CACHE_SIZE: 1536MB
      POSTGRES_MAINTENANCE_WORK_MEM: 128MB
      POSTGRES_CHECKPOINT_COMPLETION_TARGET: 0.9
      POSTGRES_WAL_BUFFERS: 16MB
      POSTGRES_DEFAULT_STATISTICS_TARGET: 100
      POSTGRES_RANDOM_PAGE_COST: 1.1
      POSTGRES_EFFECTIVE_IO_CONCURRENCY: 200
      POSTGRES_WORK_MEM: 4MB
      POSTGRES_MIN_WAL_SIZE: 1GB
      POSTGRES_MAX_WAL_SIZE: 4GB
      POSTGRES_MAX_WORKER_PROCESSES: 4
      POSTGRES_MAX_PARALLEL_WORKERS_PER_GATHER: 2
      POSTGRES_MAX_PARALLEL_WORKERS: 4
      POSTGRES_MAX_PARALLEL_MAINTENANCE_WORKERS: 2
      TZ: Asia/Shanghai
    volumes:
      # IMPORTANT: bind mount, not named volume. The updater relies on file-level snapshots.
      - ./pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U myriad -d myriad"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks: [myriad-net]
    restart: unless-stopped
    security_opt: [no-new-privileges:true]
    read_only: false
    tmpfs: [/tmp, /run]
    logging:
      driver: "json-file"
      options: { max-size: "10m", max-file: "3" }

  backend:
    # NOTE: image tag is driven by updater via ${MYRIAD_TAG}.
    image: ${BACKEND_IMAGE:-docker.io/somekawahitomi/myriad-backend}:${MYRIAD_TAG}
    container_name: myriad-backend
    deploy:
      resources:
        limits: { cpus: '2.0', memory: 2G }
        reservations: { cpus: '0.5', memory: 512M }
    environment:
      DATABASE_URL: postgres://myriad:${POSTGRES_PASSWORD}@postgres:5432/myriad
      SERVER_HOST: 0.0.0.0
      SERVER_PORT: 3000
      JWT_SECRET: ${JWT_SECRET}
      CORS_ORIGINS: ${CORS_ORIGINS:-http://localhost}
      CSP_CONNECT_SRC: ${CSP_CONNECT_SRC:-'self' https:}
      ENVIRONMENT: ${ENVIRONMENT:-production}
      FRONTEND_URL: ${FRONTEND_URL:-}
      BASE_URL: ${BASE_URL:-}
      RUST_LOG: ${RUST_LOG:-info}
      TZ: Asia/Shanghai
      MYRIAD_VERSION: ${MYRIAD_TAG}
      # Server-side updater proxy: backend forwards admin-authenticated UI requests
      # to the updater. UPDATE_TOKEN is held here, never exposed to the browser.
      MYRIAD_UPDATER_URL: http://updater:9090
      UPDATE_TOKEN: ${UPDATE_TOKEN}
    # NOTE: no `ports:` — only proxy exposes a host port.
    depends_on:
      postgres: { condition: service_healthy }
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    volumes:
      - backend_cache:/app/cache
      - backend_data:/app/data
    networks: [myriad-net]
    restart: unless-stopped
    security_opt: [no-new-privileges:true]
    read_only: false
    tmpfs: [/tmp]
    user: "0:0"
    logging:
      driver: "json-file"
      options: { max-size: "10m", max-file: "3" }

  frontend:
    image: ${FRONTEND_IMAGE:-docker.io/somekawahitomi/myriad-frontend}:${MYRIAD_TAG}
    container_name: myriad-frontend
    deploy:
      resources:
        limits: { cpus: '1.0', memory: 1G }
        reservations: { cpus: '0.25', memory: 256M }
    environment:
      PUBLIC_API_URL: ${PUBLIC_API_URL:-}
      NODE_ENV: production
      TZ: Asia/Shanghai
      MYRIAD_VERSION: ${MYRIAD_TAG}
    depends_on:
      backend: { condition: service_healthy }
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:4321"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    networks: [myriad-net]
    restart: unless-stopped
    security_opt: [no-new-privileges:true]
    read_only: true
    tmpfs: [/tmp]
    logging:
      driver: "json-file"
      options: { max-size: "10m", max-file: "3" }

  proxy:
    image: ${PROXY_IMAGE:-docker.io/somekawahitomi/myriad-proxy}:${PROXY_TAG}
    container_name: myriad-proxy
    ports:
      - "${HTTP_PORT:-80}:80"
    environment:
      PROXY_STATE_FILE: /state/maintenance.json
      PROXY_BACKEND_UPSTREAM: http://backend:3000
      PROXY_FRONTEND_UPSTREAM: http://frontend:4321
      PROXY_UPDATER_UPSTREAM: http://updater:9090
      # Direct updater channel is OFF by default. Backend /api/admin/updater/* is the
      # recommended path. Enable only for rescue scenarios when the backend is down.
      PROXY_ALLOW_DIRECT_UPDATER: ${PROXY_ALLOW_DIRECT_UPDATER:-false}
      TZ: Asia/Shanghai
    volumes:
      - ./state:/state:ro
    networks: [myriad-net]
    restart: unless-stopped
    security_opt: [no-new-privileges:true]
    read_only: true
    tmpfs: [/tmp]
    logging:
      driver: "json-file"
      options: { max-size: "10m", max-file: "3" }

  updater:
    image: ${UPDATER_IMAGE:-docker.io/somekawahitomi/myriad-updater}:${UPDATER_TAG}
    container_name: myriad-updater
    environment:
      UPDATE_TOKEN: ${UPDATE_TOKEN}
      CHANNEL: ${CHANNEL:-stable}
      GITHUB_TOKEN: ${GITHUB_TOKEN:-}
      REGISTRY_MIRROR: ${REGISTRY_MIRROR:-}
      MYRIAD_GITHUB_REPO: ${MYRIAD_GITHUB_REPO:-somekawahitomi/myriad}
      CHECK_INTERVAL_SECS: ${CHECK_INTERVAL_SECS:-3600}
      COMPOSE_PROJECT_NAME: ${COMPOSE_PROJECT_NAME:-myriad}
      MYRIAD_VERSION: ${UPDATER_TAG}
      TZ: Asia/Shanghai
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./pgdata:/host/pgdata
      - ./state:/state
      - ./backups:/backups
      - ./.env:/host/.env
      - ./:/host/compose
    networks: [myriad-net]
    restart: unless-stopped
    # NOTE: deliberately no `read_only`: updater needs to write state/snapshots/.env.
    logging:
      driver: "json-file"
      options: { max-size: "10m", max-file: "3" }

volumes:
  backend_cache: { driver: local }
  backend_data: { driver: local }

networks:
  myriad-net:
    driver: bridge
    ipam:
      config:
        - subnet: 172.28.0.0/16

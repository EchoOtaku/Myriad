# =============================================================================
# Myriad 预构建镜像部署环境配置
# =============================================================================
# 此文件用于使用 Docker Hub 预构建镜像进行部署
# =============================================================================

# -----------------------------------------------------------------------------
# Docker 镜像配置
# -----------------------------------------------------------------------------
# Docker Hub 用户名（镜像发布者）
DOCKER_USERNAME=mirai-mamori

# 镜像仓库地址
DOCKER_REGISTRY=docker.io

# 镜像标签（版本）
IMAGE_TAG=latest

# -----------------------------------------------------------------------------
# 数据库配置
# -----------------------------------------------------------------------------
POSTGRES_DB=myriad
POSTGRES_USER=myriad
# SECURITY: 必须修改此密码！
POSTGRES_PASSWORD=myriad_password_change_me
POSTGRES_PORT=5432

# -----------------------------------------------------------------------------
# 后端配置
# -----------------------------------------------------------------------------
BACKEND_PORT=3000

# Security - 必须修改！
# 生成命令: openssl rand -base64 32
JWT_SECRET=change-this-secret-in-production-use-openssl-rand-base64-32

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:4321,http://localhost:3000,http://localhost:80

# Logging
RUST_LOG=info,myriad_backend=debug
RUST_BACKTRACE=1

# -----------------------------------------------------------------------------
# 前端配置
# -----------------------------------------------------------------------------
FRONTEND_PORT=4321

# Backend API URL (accessible from browser)
PUBLIC_API_URL=http://localhost:3000

# =============================================================================
# 使用预构建镜像快速部署
# =============================================================================
# 1. 复制此文件: cp .env.prebuilt .env
# 2. 修改 POSTGRES_PASSWORD 和 JWT_SECRET
# 3. 运行: docker-compose -f docker-compose.prebuilt.yml up -d
# 4. 访问: http://localhost:4321
# =============================================================================

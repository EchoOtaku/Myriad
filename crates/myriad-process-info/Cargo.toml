[package]
name = "myriad-process-info"
version = "0.1.0"
edition = "2021"
publish = false
description = "Process memory, uptime, and build identity helpers for Myriad (agent + /health)."
authors = ["Myriad Contributors"]

[dependencies]
serde_json = "1"
